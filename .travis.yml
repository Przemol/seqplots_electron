language: node_js
node_js:
  - "4"
  
sudo: required
dist: trusty

matrix:
  include:
    - os: osx
    - os: linux

before_install:
  - ls
 
install:
  - npm install --save-dev

script:
  - ./node_modules/.bin/build
  - ./node_modules/.bin/mocha